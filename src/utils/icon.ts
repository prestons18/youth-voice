import { ThumbsUp, ThumbsDown, Send, Lightbulb, TrendingUp } from "lucide";

export const icons = {
  ThumbsUp,
  ThumbsDown,
  Send,
  Lightbulb,
  TrendingUp,
};

export type IconName = keyof typeof icons;
